# Desktop Pet 项目需求总表

日期：2026-06-02

## 1. 产品目标

开发一款面向海外市场的 Zepp OS / Amazfit 手表桌宠产品。用户抬腕即可在表盘附近看到宠物；每天的运动步数可以转化为食物，用于喂养、互动和推动成长。

产品形态采用：

1. 自定义桌宠表盘：负责抬腕可见、短动画、静态 AOD 和轻量状态展示。
2. Device Mini Program：负责步数结算、喂养、历史记录、宠物切换和收藏管理。
3. 宠物素材包：负责角色、动作、形态和元数据；为后续官方精选包和社区分享预留协议。

桌宠不能叠加到任意第三方表盘之上。首发必须提供自己的表盘方案。

## 2. 用户体验

### 2.1 抬腕即见

- 宠物常驻在自定义表盘的视觉区域内，不遮挡核心时间信息。
- 抬腕后播放一次有限帧数的短动画，然后停在静态帧。
- AOD 只显示低亮度、低像素占用的静态轮廓或静态姿势。
- 不使用无限动画、后台轮询或后台计时器。

### 2.2 运动喂养

- 读取当天累计步数。
- 每达到一个步数阈值获得一份食物，默认建议 `1000 steps = 1 food`。
- 食物每日设置上限，默认建议 `10 food/day`。
- 重复打开应用不能重复发放，也不能丢失未满阈值的进度。
- 步数下降、传感器重置、跨日和损坏数据都必须有确定性处理。

结算必须采用“当日应得总量减去今日已发量”，不能使用“本次步数减去上次打开时的步数”：

```js
const entitledFood = Math.min(DAILY_FOOD_CAP, Math.floor(steps / FOOD_PER_STEPS));
const earnedFood = Math.max(0, entitledFood - earnedToday);
```

### 2.3 初始宠物内容

Phase 1 首发目标为 `8-10` 只宠物，兼顾熟悉感与收藏感：

- 熟悉型：猫、狗、兔、仓鼠等；
- 轻幻想型：小幽灵、云朵、小星兽、史莱姆风格角色等；
- 所有角色统一为可爱、非写实、低分辨率彩色像素风；
- 角色不能直接复制现有商业桌宠的素材、命名或规则。

Phase 0 只制作一只代表性 `pixel-cat`，用于验证完整技术路径。

### 2.4 成长与分支

每只宠物至少支持三个成熟分支：

- `Active`：活跃型；
- `Steady`：稳定型；
- `Explorer`：探索型。

后续可增加 `Rare` 和可选 `Secret` 收藏形态。成长系统强调不同使用习惯产生不同结果，不惩罚低运动量用户。

### 2.5 动作协议

表盘端必须保持轻量，支持：

- `wake_idle`：抬腕短动画；
- `species_idle`：物种特色短动作；
- `tap_react`：点击反馈，若表盘点击能力不支持则迁移到 Mini Program；
- `feed`：进食；
- `happy`：开心反馈；
- `no_food`：没有食物时的反馈；
- `goal_celebrate`：达成步数目标；
- `sleep_aod`：AOD 静态轮廓；
- `evolution_hint`：进化提示；
- `secret_hint`：隐藏形态提示。

Mini Program 后续可以提供更丰富的动作，例如抚摸、玩耍、散步归来、切换形态和换装。

## 3. 轻量化要求

禁止：

- GPS；
- 心率持续监测；
- 运动模式；
- 后台轮询；
- 后台计时器；
- 无限循环表盘动画；
- AOD 高像素占用；
- 在表盘运行时下载素材。

建议：

- 抬腕动画使用有限帧和有限循环；
- 默认静态帧作为动画结束状态；
- AOD 使用静态轮廓；
- 宠物像素复杂度控制在约 `24-32 px` 角色网格，再按屏幕缩放；
- 首批覆盖圆形和方形屏幕布局；
- 以实机 `24h` 基线对比评估耗电。

## 4. 技术边界

### 4.1 Phase 0 必须验证

- Zepp OS 官方文档中的 STEP 读取方式和权限；
- Device Mini Program 的持久化方式；
- 表盘短动画能力；
- AOD 分支；
- 圆形和方形屏幕布局；
- 表盘点击能力；
- 表盘和 Mini Program 是否存在官方支持的状态共享路径；
- 包体积、资源占用和电量影响。

未验证前，不得假设表盘与 Mini Program 共享 LocalStorage，也不得假设表盘可以运行时下载素材。

### 4.2 目录责任

- `watchface-spike/`：表盘技术验证工程；
- `device-app/`：Device Mini Program；
- `device-app/core/`：Mini Program 内可测试的确定性领域逻辑；
- `pet-packs/`：宠物素材包唯一源文件；
- `watchface-spike/assets/`：由脚本生成的表盘素材镜像，不允许手工编辑；
- `core/`：仓库级素材包校验、复制和度量工具。

### 4.3 素材包与社区预留

素材包必须具备稳定元数据、动作清单、尺寸约束、锚点、资源校验和版本字段。AI 可以批量生成候选素材，但必须经过：

- 帧对齐；
- 视觉一致性检查；
- 手表实际尺寸预览；
- 包体积检查；
- 版权和相似性检查；
- 自动化协议校验。

后续社区能力优先采用“提交、审核、精选、分发”的治理模式，不在 Phase 0 实现账号、服务器目录或运行时下载。

## 5. 交付阶段

### Phase 0：可行性验证

只完成一只 `pixel-cat` Baby 形态，打通：

- 步数结算；
- 素材包协议；
- Mini Program；
- 表盘短动画；
- 静态 AOD；
- 圆形和方形构建；
- 实机验证模板；
- 包体积和电量测量。

Phase 0 必须经过至少一只圆表和一只方表的实机验证。实机结果不足时，只能记录风险和 fallback，不能伪造通过。

### Phase 1：本地完整产品

在 Phase 0 获得 `Go` 决策后，开发 `8-10` 只宠物、三分支成长、Rare / Secret 收藏、本地历史和完整管理体验。

### Phase 2：官方精选素材包

增加更多审核后的宠物包、动作和轻量换装。

### Phase 3：社区素材分享

在分发路径和治理方案明确后，再开发 Side Service、账号、素材目录、提交流程、审核和内容治理。

## 6. Phase 0 验收条件

Phase 0 报告必须包含：

1. 官方 API 验证链接和结论；
2. 自动化测试结果；
3. 圆表与方表构建结果；
4. 实机 STEP 真实性；
5. 抬腕动画是否只播放一次并停止；
6. AOD 是否静态且低像素；
7. 表盘点击能力结论和 fallback；
8. 状态共享能力结论和 fallback；
9. 包体积、资源统计和 `24h` 电量对比；
10. `Go / Conditional Go / No-Go` 决策。

## 7. 执行入口

AI 执行者依次阅读：

1. `AGENTS.md`
2. `docs/PROJECT_BOARD.md`
3. `docs/superpowers/specs/2026-06-02-zepp-pet-universe-design.md`
4. `docs/superpowers/plans/2026-06-02-zepp-pet-universe-phase-0-feasibility.md`
5. `docs/superpowers/runbooks/2026-06-02-zepp-pet-universe-ai-execution-runbook.md`
6. `docs/prompts/00-orchestrator.md`
