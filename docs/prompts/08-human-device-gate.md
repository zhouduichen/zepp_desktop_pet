# Prompt: Human Physical-Watch Gate

```text
Perform the Zepp Pet Universe Phase 0 physical-device gate.

Workspace: D:\huami\desktop_pet

Read:
- docs/templates/phase-0-device-spike-template.md
- watchface-spike/README.md

Copy the template to:
docs/validation/phase-0-device-spike.md

Use:
- one round Zepp OS API_LEVEL >= 3.0 watch;
- one square Zepp OS API_LEVEL >= 3.0 watch.

Install the watch-face spike and Device Mini Program with the official Zeus workflow.

Record for each watch:
1. model, resolution, and Zepp OS version;
2. STEP daily total accuracy;
3. raise-to-wake finite animation behavior;
4. static idle behavior after animation;
5. pet tap behavior;
6. static AOD silhouette;
7. one-time food settlement without duplicate claim;
8. any documented watch-face / Mini Program state-sharing behavior;
9. package bytes, representative pack bytes, memory observations, AOD lit-pixel ratio;
10. baseline versus pet-face battery observations over comparable 24-hour periods with
    the same AOD and raise-to-wake settings.

Do not generalize one model's battery result to every watch.
Select fallback checkboxes and write an explicit Go / No-Go recommendation.
```
